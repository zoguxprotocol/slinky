version: v2

modules:
  - path: vendors/mexc/websocket-proto
    name: buf.build/zoguxprotocol/vendors-mexc-proto
    lint:
      except:
        - FILE_LOWER_SNAKE_CASE
        - FIELD_LOWER_SNAKE_CASE
        - PACKAGE_DEFINED

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD

breaking:
  use:
    - PACKAGE
